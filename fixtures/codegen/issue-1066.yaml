openapi: 3.0.0
info:
  description: description
  title: title
  version: 1.0.0
paths:
  /path:
    post:
      description: description
      tags:
        - path
      operationId: path.post
      requestBody:
        content:
          application/json:
            schema:
              required:
                - params
              properties:
                params:
                  type: object
                  required:
                    - field
                  properties:
                    field:
                      description: field
                      type: integer
      responses:
        "200":
          description: description
          content:
            application/json:
              schema:
                type: object
                required:
                  - result
                properties:
                  result:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: id
                          type: integer
servers:
  - url: /
