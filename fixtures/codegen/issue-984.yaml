openapi: 3.0.0
info:
  title: Example Endpoint
  version: 0.0.1
paths:
  /xxx/add:
    post:
      description: redacted
      responses:
        "204":
          description: OK
      requestBody:
        content:
          application/json:
            schema:
              properties:
                retest:
                  properties:
                    id:
                      type: string
        required: true
servers:
  - url: https://example.com/v1
