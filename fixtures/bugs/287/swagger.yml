openapi: 3.0.0
info:
  version: 1.0.0
  title: Test
paths:
  "/key/{id}":
    delete:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
components:
  schemas:
    example:
      type: object
      required:
        - begin
      properties:
        begin:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        name:
          type: string
