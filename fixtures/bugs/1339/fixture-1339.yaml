openapi: 3.0.0
info:
  title: postBin
  version: x
paths:
  /bin:
    post:
      operationId: postBin
      requestBody:
        content:
          application/json:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: success
