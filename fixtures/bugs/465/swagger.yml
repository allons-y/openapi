openapi: 3.0.0
info:
  version: 1.0.0
  title: Test
paths:
  "/key/{id}":
    delete:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
servers:
  - url: /v1/fancyAPI
