openapi: 3.0.0
info:
  version: 0.0.1
  title: Test
paths:
  /test:
    put:
      operationId: test
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/Value"
        required: true
      tags:
        - Test
      responses:
        default:
          description: successful operation
servers:
  - url: /v1
components:
  schemas:
    Value:
      type: object
      discriminator:
        propertyName: ValueType
      required:
        - ValueType
      properties:
        ValueType:
          type: string
    StringValue:
      allOf:
        - $ref: "#/components/schemas/Value"
        - type: object
          properties:
            foobar:
              type: string
