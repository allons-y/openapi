openapi: 3.0.0
info:
  description: Test
  title: Test
  version: 0.0.1
paths:
  /test/a:
    $ref: "#/components/schemas/schema1"
  /test:
    $ref: "#/components/schemas/schema1"
servers:
  - url: /v1
components:
  schemas:
    schema1:
      get:
        responses:
          "200":
            description: OK
