openapi: 3.0.0
info:
  title: issue-342
  version: 0.0.1
  license:
    name: MIT
paths:
  /get_main_object:
    get:
      tags:
        - maindata
      parameters:
        - $ref: "#/components/parameters/sid"
      responses: {}
servers:
  - url: http://localhost:8081/api/v1
components:
  parameters:
    sid:
      name: sid
      in: query
      required: true
      schema:
        type: integer
        format: int64
