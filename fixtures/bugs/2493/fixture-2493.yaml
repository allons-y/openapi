openapi: 3.0.0
info:
  description: Test
  version: 0.0.1
  title: Test
paths:
  /test:
    post:
      operationId: test
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestRequest"
        required: true
      responses:
        "200":
          description: OK
servers:
  - url: http://test/test/v1
components:
  schemas:
    TestRequest:
      type: object
      properties:
        name:
          type: string
