openapi: 3.0.0
info:
  title: Validation Issue
  description: Endpoints
  version: 0.0.1
paths:
  /events:
    post:
      operationId: postEvents
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Event"
        description: Event to post
        required: true
      responses:
        "201":
          $ref: "#/components/responses/CreatedEventResponse"
servers:
  - url: /v1
components:
  responses:
    CreatedEventResponse:
      description: Successfully created an event
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Event"
  schemas:
    Event:
      type: string
