openapi: 3.0.0
info:
  version: 0.0.0
  title: Breaking API as described by emsa23
paths:
  /:
    get:
      operationId: getFoo
      responses:
        "200":
          $ref: "#/components/responses/200"
components:
  responses:
    "200":
      description: ok
      headers:
        status:
          schema:
            type: array
            items:
              type: string
              enum:
                - NEW
                - PUBLISHED
                - OPEN
                - FINISHED
          style: simple
