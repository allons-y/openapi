openapi: 3.0.0
info:
  title: fixture for issue#909
  version: 1.0.0
paths:
  /optional:
    get:
      operationId: getOptional
      parameters:
        - name: notAnOption1
          in: query
          required: true
          style: form
          explode: false
          schema:
            type: array
            items:
              type: array
              items:
                type: string
                format: date-time
          x-s2o-warning: Nested collectionFormats are not supported
        - name: isAnOption2
          in: query
          required: false
          style: pipeDelimited
          schema:
            type: array
            items:
              type: array
              items:
                type: string
                format: uuid
        - name: isAnOption4
          in: query
          required: false
          style: form
          explode: false
          schema:
            type: array
            items:
              type: array
              uniqueItems: true
              items:
                type: array
                uniqueItems: true
                minItems: 3
                items:
                  type: string
                  format: uuid
                  enum:
                    - a
                    - b
                    - c
            maxItems: 4
          x-s2o-warning: Nested collectionFormats are not supported
        - name: isAnOptionalHeader
          in: header
          required: false
          x-s2o-warning: collectionFormat:pipes is no longer supported except for in:query
            parameters
          schema:
            type: array
            items:
              type: array
              items:
                type: string
                format: uuid
            uniqueItems: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContainerConfig"
        required: true
      responses:
        "200":
          description: simple type
          content:
            application/json:
              schema:
                type: string
                format: date-time
        "201":
          description: simple type
          content:
            application/json:
              schema:
                type: string
                format: uuid
        default:
          description: schema type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContainerConfig"
servers:
  - url: http://localhost
components:
  schemas:
    ContainerConfig:
      type: object
      required:
        - config1
      properties:
        config1:
          type: string
          format: date-time
        config2:
          type: string
          format: uuid
