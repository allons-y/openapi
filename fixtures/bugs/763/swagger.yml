openapi: 3.0.0
info:
  title: Test
  description: Test Service
  version: 1.0.0
paths:
  /myMethod:
    get:
      operationId: get_test
      summary: Get a list of int32s.
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/test_list"
servers:
  - url: http://localhost
components:
  schemas:
    test_list:
      type: object
      properties:
        the_array:
          type: array
          items:
            minimum: 0
            maximum: 10
            type: integer
            format: int32
