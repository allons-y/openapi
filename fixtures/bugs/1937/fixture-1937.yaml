openapi: 3.0.0
info:
  title: error codes >= 512
  version: 0.0.1
  description: repro issue 1893
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
paths:
  /getRecords:
    get:
      operationId: getRecords
      parameters:
        - name: 101param
          in: query
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/400Schema"
        required: true
      responses:
        "200":
          description: OK
          headers:
            101header:
              schema:
                type: string
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/400Schema"
components:
  schemas:
    400Schema:
      type: string
