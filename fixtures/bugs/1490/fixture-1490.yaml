openapi: 3.0.0
info:
  title: interface body
  version: 0.0.1
  description: repro issue 1490
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
paths:
  /getRecords:
    get:
      operationId: getRecords
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/modelInterface"
        required: true
      responses:
        "200":
          description: OK
  /getMoreRecords:
    get:
      operationId: getMoreRecords
      requestBody:
        content:
          application/json:
            schema:
              type: object
        required: true
      responses:
        "200":
          description: OK
  /getRecordsNonRequired:
    get:
      operationId: getRecordsNonRequired
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/modelInterface2"
      responses:
        "200":
          description: OK
components:
  schemas:
    modelInterface:
      type: object
      additionalProperties: true
    modelInterface2:
      type: object
