openapi: 3.0.0
info:
  title: test for upload
  version: 0.0.1
paths:
  /files/:
    post:
      summary: upload the files.
      operationId: uploadFile
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                body:
                  type: string
                  format: binary
              required:
                - body
        required: true
      responses:
        default:
          description: default response
