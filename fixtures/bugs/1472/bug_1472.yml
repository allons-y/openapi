openapi: 3.0.0
info:
  title: My
  version: 1.0.0
paths:
  /endpoint:
    get:
      security:
        - opBearer: []
      operationId: getEndpoint
      tags:
        - ops
      responses:
        "200":
          description: OK
servers:
  - url: //example.com
components:
  securitySchemes:
    opBearer:
      in: header
      name: Authorization
      type: apiKey
      x-go-name: OpBearer
