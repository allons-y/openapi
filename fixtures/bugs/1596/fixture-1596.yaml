openapi: 3.0.0
info:
  version: 1.0.0
  title: MyApp
  description: (Description pending)
paths:
  /hello:
    get:
      summary: Simple Hello World endpoint
      tags:
        - Hello
      responses:
        "200":
          description: service is saying hello
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/somethingEndingInWindows"
components:
  schemas:
    somethingEndingInWindows:
      type: object
