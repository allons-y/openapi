openapi: 3.0.0
info:
  description: example in response test
  title: expand test
  version: 0.0.0
paths:
  /test:
    get:
      operationId: optest
      responses:
        "200":
          description: Success
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/Response"
            application/json:
              examples:
                response:
                  value:
                    test: blah
  /test2:
    get:
      operationId: optest2
      responses:
        "200":
          description: Success
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/Response"
components:
  schemas:
    Response:
      type: object
      properties:
        test:
          type: string
