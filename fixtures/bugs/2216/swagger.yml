openapi: 3.0.0
info:
  title: Test API
  description: Test
  version: v1
paths:
  /obj:
    get:
      summary: Get an object
      operationId: getObject
      responses:
        "200":
          description: The object
          content:
            application/json:
              schema:
                $ref: ./defs.yml#/definitions/MyObject
servers:
  - url: https://host/path
  - url: http://host/path
