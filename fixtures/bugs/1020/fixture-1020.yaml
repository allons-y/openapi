openapi: 3.0.0
info:
  description: someTest
  title: test
  version: 0.0.0
paths:
  /upload:
    post:
      operationId: someTest
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  description: The file to upload
                  type: string
                  format: binary
              required:
                - file
        required: true
      responses:
        "200":
          description: Successful upload
          content:
            application/json:
              schema:
                type: string
