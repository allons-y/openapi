openapi: 3.0.0
info:
  version: 1.0.0
  title: API
paths:
  /test:
    post:
      operationId: testPost
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostRequest"
        required: true
      responses:
        "201":
          description: Test
          content:
            "*/*":
              schema:
                type: object
                properties:
                  msg:
                    type: string
                    x-isnullable: false
components:
  schemas:
    PostRequest:
      $ref: "#/components/schemas/PostFields"
    PostFields:
      type: object
      required:
        - a
        - b
      properties:
        a:
          type: string
          x-isnullable: false
        b:
          type: string
    Composed:
      $ref: "#/components/schemas/NoValidations"
    NoValidations:
      type: object
      properties:
        c:
          type: string
    ComposedIface:
      $ref: "#/components/schemas/Interface"
    Interface:
      type: object
