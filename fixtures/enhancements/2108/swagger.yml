openapi: 3.0.0
info:
  version: 1.0.0
  title: Example
paths:
  /test:
    get:
      summary: Says hello
      responses:
        "200":
          description: A hello world string
          content:
            text/plain:
              schema:
                type: string
