openapi: 3.0.0
info:
  version: 1.0.0
  title: issue 1232
  description: verifies marshalling code on discriminated type
paths:
  /sanity:
    get:
      operationId: getSanity
      description: no description
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModelObjectVanilla"
      responses:
        default:
          description: Generic Out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelObjectVanilla"
components:
  schemas:
    ModelObjectVanilla:
      type: object
      required:
        - prop1
      properties:
        prop0:
          $ref: "#/components/schemas/ModelSanity"
        prop1:
          $ref: "#/components/schemas/ModelSanity"
        prop2:
          type: array
          items:
            $ref: "#/components/schemas/ModelSanity"
        prop3:
          $ref: "#/components/schemas/ModelSanity"
        prop4:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ModelSanity"
        prop5:
          type: integer
      additionalProperties:
        type: object
        additionalProperties:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ModelSanity"
    ModelSanity:
      type: object
      required:
        - propB
      properties:
        propA:
          type: string
        propB:
          type: string
