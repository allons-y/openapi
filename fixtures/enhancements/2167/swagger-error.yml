openapi: 3.0.0
info:
  version: 1.0.0
  title: Example
paths:
  "/test/{test_name}":
    get:
      operationId: xGoNameInParams
      summary: Says hello
      parameters:
        - in: path
          name: test_name
          x-go-name:
            - bad
          required: true
          schema:
            type: string
      responses:
        "200":
          description: A hello world string
          content:
            text/plain:
              schema:
                type: string
