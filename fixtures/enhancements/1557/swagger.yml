openapi: 3.0.0
info:
  version: 1.0.0
  title: Example
paths:
  /pdf:
    post:
      summary: Accepts and returns a pdf file
      responses:
        "200":
          description: A pdf file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            application/dummy:
              schema:
                type: string
                format: binary
